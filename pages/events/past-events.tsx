import React from "react";

// Importing components
import MetaHead from "@/components/MetaHead";

const PastEvents: React.FunctionComponent = () => {
    return (
        <React.Fragment>
            <MetaHead title="Past Events - Hackathons, Sessions & Events" />
        </React.Fragment>
    )
};

export default PastEvents;